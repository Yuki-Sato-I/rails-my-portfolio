<div id="tabProductions" class="mytab">
  <h2>作品(<%= @productions.count %> productions)</h2>
  <ul>
    <% @productions.each do |production| %>
      <li class="my_production">
        <p class="title-name"><%= production.name %></p>
        <h4>[内容]</h4>
        <div class="my-content">
          <p><%= simple_format(production.content) %></p>
        </div>
        <div>リンク:<%= link_to "#{production.name}", "#{production.link}" %></div>
        <div>使用スキル:
          <% production.languages.each do |language| %>
            <%= link_to "#{language.name}", "/language/#{language.id}" %>
          <% end %>
        </div>
        <p class="update">最終更新日:<%= production.complete_date %></p>
      </li>
    <% end %>
  </ul>
</div>



<!--
  <div class="my_production">
    <h2 class="title-name"><%= @productions.name %></h2>
    <p>リンク:<%= link_to "#{@productions.name}", "https://tec-board-styk.herokuapp.com/" %></p>
    <p>使用言語=> Ruby(on rails), HTML&CSS</p> 
    <p>最終更新日:2018/12/01</p>
  </div>
  <div class="my_production">
    <h2 class="title-name">LineBot:お天気チェック</h2>
    <%= image_tag "no_image.jpg" %>
    <p>使用言語=> Ruby(on rails)</p>
    <p>最終更新日:2018/12/04</p>
  </div>
  <div class="my_production">
    <h2 class="title-name">ポートフォリオサイト</h2>
    <p>リンク:<%= link_to "Yuki's PortFolio", "https://my-portfolio-styk.herokuapp.com/" %></p>
    <p>使用言語=> Ruby(on rails), HTML&CSS, JavaScript </p>
    <p>最終更新日:2018/12/08</p>
  </div>
  <div class="my_production">
    <h2 class="title-name">C-Bonds</h2>
    <p>リンク:<%= link_to "C-Bonds", "https://c-bonds-styk.herokuapp.com" %></p>
    <p>使用言語=> Ruby(on rails), HTML&CSS, JavaScript </p>
    <p>最終更新日:2018/12/24</p>
  </div>
</div>
-->